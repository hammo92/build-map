/*if (property.type === 'title') {
    if (!property.useTemplate || !property.stringTemplate) {
        newContent.title = ''
    }
    const string = property.stringTemplate!.map((element) => {

    })
}*/
