import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
    imageWrapper: {
        background:
            "rgba(0, 0, 0, 0) repeating-conic-gradient(rgba(246, 246, 249, 0.04) 0%, rgba(246, 246, 249, 0.04) 25%, transparent 0%, transparent 50%) repeat scroll 50% center / 20px 20px",
    },
}));
